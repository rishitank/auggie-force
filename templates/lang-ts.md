TypeScript guidance:
- Prefer strict types, no implicit any; export types for APIs.
- Narrow types with guards; avoid type assertions when possible.
- Keep generics minimal and clear; enforce exhaustive switch.

