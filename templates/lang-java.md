Java guidance:
- Prefer immutability; validate nullability and Optional usage.
- Use SLF4J logging; avoid System.out.
- Ensure JUnit tests cover error paths; avoid blocking calls on virtual threads.

