Security sweep:
- Detect secrets, unsafe deserialization, injection, SSRF, XSS, CSRF, path traversal.
- Enforce authz checks around admin/privileged code.
- Require parameterized queries and safe file handling.
- Provide concrete remediation patches.

