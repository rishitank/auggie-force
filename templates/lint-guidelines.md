Lint & style pass (fast path):
- Catch formatting, naming, unused code, minor smells.
- Suggest minimal diffs; avoid large refactors.
- If the repo uses formatters/linters, align suggestions to those tools.

